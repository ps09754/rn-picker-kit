import { View, Text, ScrollView } from 'react-native'
import React from 'react'
import { PickerProps } from '../../Strings/interfaceDefault'

const PickerPopup = ({ ...props }: PickerProps) => {
    return (
        <View>

        </View>
    )
}

export default PickerPopup